name: Movie_App_Flutter
description: Proyecto de peliculas, donde se consume una API, para las peliculas.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.1.1 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  cupertino_icons: ^1.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.2
  build_runner: ^2.4.6
  equatable: ^2.0.5
  dartz: ^0.10.1
  flutter_bloc: ^8.1.3
  get_it: ^7.6.4
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true